# 010: Small Wins Dashboard（基本版）

## 概要

プロセスメトリクスを可視化する基本的なダッシュボードを実装する。Day21の行動継続ティア表示に必要な最小限の実装。

## Phase

**Phase A: MVP**

## 優先度

中

## 依存関係

- 前提: 003 ホーム画面, 006 記憶システム
- 後続: 007 Day21 Judgment Gate

---

## 機能要件（MVP版）

### 1. 基本メトリクス（MVP必須）

| メトリクス | 説明 | 計算方法 |
|-----------|------|---------|
| チェックイン継続率 | チェックインした日数 / 経過日数 | 自動計算 |
| チェックイン連続日数 | 現在の連続チェックイン日数 | 自動計算 |
| **If-Then発動回数** ← 追加 | 障害に遭遇してIf-Thenを実行した回数 | チェックイン時の記録 |
| **If-Then発動率** ← 追加 | If-Then発動回数 / チェックイン回数 | 自動計算 |
| Evidence提出数 | 証拠の累計数 | カウント |
| Evidence提出率 | Evidence提出数 / 期待数（週1想定） | 自動計算 |
| コミット履行率 | 完了コミット / 全コミット | 自動計算 |

**要件背景:**
- requirements.md で「If-Then発動回数」が必須プロセスメトリクスとして指定
- 007 Day21でIf-Then発動率が行動継続ティア算出に必要

**実装方針（MVP簡易版）:**
- **記録**: チェックイン時に実施（004 音声チェックイン / 003 テキストチェックイン共通）
  - チェックイン完了時に「今日、予定していた障害に遭遇しましたか？」を質問
  - 遭遇した場合「If-Thenを実行しましたか？」で記録
  - AI自動判定は補助（チェックイン内容から「誘惑」「障害」「乗り越えた」等のキーワード検出）
  - `checkins.if_then_triggered = true` として保存
- **計算**: 010 Small Wins（本モジュール）で集計・可視化
  - checkinsテーブルから `if_then_triggered = true` の件数をカウント
  - If-Then発動率 = If-Then発動回数 / チェックイン回数

> 深掘り分析（障害タイプ別、成功/失敗率等）は Phase B で追加

### 2. ティア表示（Day21用）

| ティア | 条件 | 表示 |
|--------|------|------|
| On Track | 平均率 >= 70% | 「今のペースを維持できています」 |
| At Risk | 平均率 >= 40% | 「一部の行動に注意が必要です」 |
| Needs Reset | 平均率 < 40% | 「一度立ち止まって再設計しましょう」 |

### 3. 算出根拠の明示（必須）

**Day21向けの完全版:**
```
あなたの行動継続: On Track
────────────────────────
算出根拠:
・チェックイン継続率: 19/21日 (90%)
・If-Then発動率: 8/12回 (67%)
・Evidence提出率: 5/6件 (83%)
・コミット履行率: 14/17件 (82%)
────────────────────────
平均: 80.5% → On Track
※ このスコアは行動指標であり、成果を保証しません
```

**ホーム画面サマリー版（簡易）:**
```
今週の進捗: 5/7日
If-Then発動: 3回
Evidence: 2件
```

---

## UI設計（MVP版）

### ホーム画面のサマリー表示

```
┌─────────────────────────────┐
│  📊 今週の進捗              │
│  チェックイン: 5/7日         │
│  Evidence: 2件              │
│  コミット完了: 8/10          │
│  [詳細を見る]               │
└─────────────────────────────┘
```

### ダッシュボード画面

```
┌─────────────────────────────┐
│  Small Wins                  │
├─────────────────────────────┤
│                              │
│  🔥 連続チェックイン: 12日   │
│                              │
│  📊 全期間の実績             │
│  ┌───────────────────────┐  │
│  │ チェックイン: 19/21日  │  │
│  │ ━━━━━━━━━━━━━━━━━░░░ 90% │
│  │                       │  │
│  │ Evidence: 5件         │  │
│  │ ━━━━━━━━━━━━━━━━░░░░ 83% │
│  │                       │  │
│  │ コミット完了: 14/17   │  │
│  │ ━━━━━━━━━━━━━━━━░░░░ 82% │
│  └───────────────────────┘  │
│                              │
│  行動継続ティア: On Track    │
│  「今のペースを維持できています」│
│                              │
└─────────────────────────────┘
```

---

## データモデル

既存テーブルを使用:
- `checkins` - チェックイン記録
- `evidences` - Evidence記録
- `commitments` - コミットメント記録

### 追加: checkins テーブルへのカラム追加

```sql
ALTER TABLE checkins
ADD COLUMN if_then_triggered BOOLEAN DEFAULT false;
-- チェックイン時に「障害に遭遇してIf-Thenを実行した」フラグ
```

### 追加: small_wins_cache テーブル（パフォーマンス用）

```sql
CREATE TABLE small_wins_cache (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES users(id) NOT NULL UNIQUE,
  checkin_count INTEGER DEFAULT 0,
  checkin_streak INTEGER DEFAULT 0,
  if_then_count INTEGER DEFAULT 0, -- ← 追加
  evidence_count INTEGER DEFAULT 0,
  commitment_completed INTEGER DEFAULT 0,
  commitment_total INTEGER DEFAULT 0,
  tier VARCHAR(20) DEFAULT 'on_track',
  updated_at TIMESTAMP DEFAULT NOW()
);
```

---

## API設計

### GET /api/small-wins/summary

サマリーデータを取得

**Response:**
```json
{
  "metrics": {
    "checkin_rate": 0.9,
    "checkin_count": 19,
    "checkin_total": 21,
    "checkin_streak": 12,
    "if_then_count": 8,
    "if_then_rate": 0.67,
    "evidence_count": 5,
    "evidence_rate": 0.83,
    "commitment_completed": 14,
    "commitment_total": 17,
    "commitment_rate": 0.82
  },
  "tier": "on_track",
  "tier_message": "今のペースを維持できています",
  "average_rate": 0.805
}
```

---

## メトリクス計算

### チェックイン継続率

```typescript
async function calculateCheckinRate(userId: string): Promise<number> {
  const user = await getUser(userId);
  const daysSinceStart = getDaysSince(user.trial_start_date);

  const { count } = await supabase
    .from('checkins')
    .select('id', { count: 'exact' })
    .eq('user_id', userId);

  return count / daysSinceStart;
}
```

### チェックイン連続日数

```typescript
async function calculateCheckinStreak(userId: string): Promise<number> {
  const checkins = await supabase
    .from('checkins')
    .select('date')
    .eq('user_id', userId)
    .order('date', { ascending: false });

  let streak = 0;
  let expectedDate = new Date();

  for (const checkin of checkins.data) {
    const checkinDate = new Date(checkin.date);
    if (isSameDay(checkinDate, expectedDate)) {
      streak++;
      expectedDate.setDate(expectedDate.getDate() - 1);
    } else {
      break;
    }
  }

  return streak;
}
```

### If-Then発動回数

```typescript
async function calculateIfThenCount(userId: string): Promise<number> {
  const { count } = await supabase
    .from('checkins')
    .select('id', { count: 'exact' })
    .eq('user_id', userId)
    .eq('if_then_triggered', true);

  return count;
}
```

### ティア判定（Day21準拠）

```typescript
interface ProgressMetrics {
  checkinRate: number;      // チェックイン継続率
  ifThenRate: number;       // If-Then発動率
  evidenceRate: number;     // Evidence提出率
  commitmentRate: number;   // コミット履行率
}

function calculateTier(metrics: ProgressMetrics): 'on_track' | 'at_risk' | 'needs_reset' {
  // 007-day21-judgment-gate.md と同一ロジック
  const avg = (
    metrics.checkinRate +
    metrics.ifThenRate +
    metrics.evidenceRate +
    metrics.commitmentRate
  ) / 4;

  if (avg >= 0.7) return 'on_track';
  if (avg >= 0.4) return 'at_risk';
  return 'needs_reset';
}
```

---

## Todo

### データ
- [x] checkins テーブルに if_then_triggered カラム追加（初期スキーマで実装済み）
- [ ] small_wins_cache テーブル作成（オプション・Phase B）
- [ ] キャッシュ更新トリガー（オプション・Phase B）

### メトリクス計算
- [x] チェックイン継続率計算
- [x] チェックイン連続日数計算
- [x] If-Then発動回数カウント
- [x] If-Then発動率計算
- [x] Evidence提出数カウント
- [x] Evidence提出率計算
- [x] コミット履行率計算
- [x] ティア判定ロジック（4メトリクス平均版）

### UI実装
- [x] ホーム画面サマリーコンポーネント（SmallWinsSummary）
- [x] ダッシュボード画面（dashboard.tsx）
- [x] プログレスバーコンポーネント
- [x] ティア表示カード
- ~~チェックイン時のIf-Then記録UI~~ → 004 音声チェックインで実装済み

### API実装
- [x] GET /api/small-wins/summary（全メトリクス対応）
- ~~チェックイン時のif_then_triggered保存処理~~ → 004 音声チェックインで実装済み

**実装完了: 2026-01-13**

---

## 既知の制限事項（MVP）

### If-Then発動率の計算
- **現行**: `if_then_triggered回数 / 全チェックイン数`
- **理想**: `if_then_triggered回数 / 障害遭遇回数`
- **Phase B対応**: `obstacle_encountered` フラグを追加し、障害遭遇時の実行成功率を計測

### 週次データ表示
- **現行**: 全期間の累計データを表示
- **Phase B対応**: 週単位のフィルタリングを追加

### ティア計算の4指標
1. チェックイン継続率
2. If-Then発動率
3. Evidence提出率
4. コミット履行率

※ 連続日数（streak）は表示のみで、ティア平均には含まない

---

## 実装ファイル一覧

### メトリクス計算
- `lib/metrics/small-wins.ts` - メトリクス計算ロジック、ティア判定

### API
- `app/api/small-wins/summary+api.ts` - メトリクスサマリーAPI

### フック
- `hooks/data/useSmallWins.ts` - Small Winsデータフック

### UIコンポーネント
- `components/dashboard/SmallWinsSummary.tsx` - ホーム画面用サマリー
- `app/(tabs)/dashboard.tsx` - ダッシュボード画面

---

## 完了条件

1. 基本メトリクス（チェックイン率、**If-Then発動率**、Evidence率、コミット率）が計算される
2. チェックイン連続日数が表示される
3. チェックイン時にIf-Then発動を記録できる
4. ティアが4メトリクス平均で判定・表示される
5. 算出根拠（4メトリクス）が明示される
6. ホーム画面にサマリーが表示される
7. Day21レポート用のデータが正しく取得できる
